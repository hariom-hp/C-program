#include<stdio.h>
int main()
{
    int n;
    printf("eneter the any two number ");
    scanf("%d",&n);
    if(n%2==0)
    printf("no even");
    else
    printf("no is odd");
    return 0;
    
}