#include<stdio.h>
int main(){
    int length,width,area;
    printf("enter the any two nuber ");
    scanf("%d%d",&length,&width);
    area=length*width;
    printf("rectangle is  is :%d",area);
    return 0;
}